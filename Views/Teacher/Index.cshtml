@model IEnumerable<Teacher>

<div class="album py-5 bg-body-tertiary">
    <div class="container">

        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
            @foreach (var item in Model)
            {
                <div class="col">
                    <div class="card shadow-sm">
                        <img class="bd-placeholder-img card-img-top" width="100%" height="225" src="@item.Image" role="img" focusable="false"></img>
                        <div class="card-body">
                            <p class="card-text">@item.Name</p>
                            <p class="card-text">@item.Surname</p>
                            <p class="card-text">@item.DetailInformation</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a type="button" class="btn btn-sm btn-outline-secondary" asp-action="Detail" asp-controller="Teacher" asp-route-id="@item.Id">View</a>
                                    <a type="button" class="btn btn-sm btn-outline-secondary" asp-action="Edit" asp-controller="Teacher" asp-route-id="@item.Id">Edit</a>
                                    <a type="button" class="btn btn-sm btn-outline-secondary" asp-action="Delete" asp-controller="Teacher" asp-route-id="@item.Id">Delete</a>
                                </div>
                                <small class="text-body-secondary">@item.CourseCategory</small>
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</div>
